[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers --ignore=tests/_DEPRECATED
markers =
    invariant: Polaris invariant test (P7 property-based)
    integration: cross-module integration
    slow: slow tests (skip in fast loop)
